# Web Standards

- Accessibility: WCAG 2.1 AA compliance required
- Security: CSP headers required on all pages
- No inline scripts or styles (CSP violation)
- Validate all user input server-side, sanitize for display client-side
- HTTPS only — no mixed content
